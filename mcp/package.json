{
  "name": "@openleads/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenLeads MCP server — wraps the OpenLeads public API (/api/v1) over stdio.",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "node --import tsx --test src/client.test.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
